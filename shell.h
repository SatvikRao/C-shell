#ifndef SHELL_H
#define SHELL_H

/**
 * Initialize the shell with required settings
 * Returns 0 on success, non-zero on failure
 */
int initialize_shell();

/**
 * Run the main shell loop
 */
void run_shell();

#endif // SHELL_H
